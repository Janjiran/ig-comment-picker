{
    "actorOutputSchemaVersion": 1,
    "title": "Instagram Comment Picker output",
    "properties": {
        "reveal": {
            "type": "string",
            "title": "🎬 Watch the winner reveal",
            "description": "An animated, shareable winner reveal — perfect for recording the live draw for your audience.",
            "template": "https://giveaways-picker.com/reveal?run={{links.apiRunUrl}}"
        },
        "winners": {
            "type": "string",
            "title": "🏆 Winners",
            "description": "The randomly selected winning comments, ready to announce.",
            "template": "{{links.apiDefaultDatasetUrl}}/items?view=winners"
        },
        "summary": {
            "type": "string",
            "title": "Run summary",
            "description": "Winners plus stats: total comments read, how many were eligible, and the filters applied — proof the draw was fair.",
            "template": "{{links.apiDefaultKeyValueStoreUrl}}/records/OUTPUT"
        }
    }
}
